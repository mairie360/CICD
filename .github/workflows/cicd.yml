name: Test Workflow

on:
  workflow_call:
    inputs:
      type:
        required: true
        type: string
        description: 'Back or frontend deployment type'
      postman_collection:
        required: false
        type: string
        description: 'Collection id for postman tests'
    secrets:
      CPANEL_API_TOKEN:
        required: false
      CPANEL_PASSWORD:
        required: false
      CPANEL_SERVER:
        required: false
      CPANEL_USERNAME:
        required: false
      POSTMAN_API_KEY:
        required: false
      DEPLOY_SSH_KEY:
        required: false
      MAIRIE_360_DEPLOY_TOKEN:
        required: false

jobs:

  run-back:
    if: ${{ inputs.type == 'back' }}
    uses: ./.github/workflows/back_ci.yml
    with:
      postman_collection: ${{ inputs.postman_collection }}
    secrets:
      POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}

  run-front:
    if: ${{ inputs.type == 'front' }}
    uses: ./.github/workflows/front_ci.yml

  run-api-lib-ci:
    if: ${{ inputs.type == 'ci-back-lib' }}
    uses: ./.github/workflows/ci-back-lib.yml

  run-api-lib-cd:
    if: ${{ inputs.type == 'cd-back-lib' }}
    uses: ./.github/workflows/cd-back-lib.yml
    secrets:
      CPANEL_API_TOKEN: ${{ secrets.CPANEL_API_TOKEN }}
      CPANEL_PASSWORD: ${{ secrets.CPANEL_PASSWORD }}
      CPANEL_SERVER: ${{ secrets.CPANEL_SERVER }}
      CPANEL_USERNAME: ${{ secrets.CPANEL_USERNAME }}
      MAIRIE_360_DEPLOY_TOKEN: ${{ secrets.MAIRIE_360_DEPLOY_TOKEN }}
